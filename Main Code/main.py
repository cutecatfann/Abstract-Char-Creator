from characterCreator import Character

"""
Main interface for interacting with the user. 

It allows the user to select if they want to generate a random character, or build their own.

It also displays menu with user option.

This file also displays the returns for the user. It keeps the user contained in one file.
"""
# dict contains all the attributes returned by the called classes
# dict: character = {}

# firstInput = input()

# while firstInput = true: 
    # if firstInput == 1:
        # random character to CharacterCreator
    # elif firstInput == 2:
        # user inputted character to CharacterCreator
    # elif firstInput = 3:
        # quit program
    # else:
        # exception

    # format and print 

    # ask if the user wants to run the program again
